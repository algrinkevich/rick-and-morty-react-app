import { HeaderContainer, Logo } from "./styles";

const Header = () => {
  return (
    <HeaderContainer>
      <Logo src="/src/assets/logo.svg" />
    </HeaderContainer>
  );
};

export default Header;
